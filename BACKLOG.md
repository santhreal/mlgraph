# BACKLOG - mlgraph
src/op/mod.rs:234 | bug | high | Split shape inference returns output shapes identical to input shape instead of adjusting split dimension size | FIXED: per-section output shape sets the (axis-normalized) split dim to the section size; validates sections sum == dim size; saturating sum guards overflow. Proving tests split_infers_per_section_shapes / split_rejects_section_sum_mismatch / split_supports_negative_dim. | status=done
src/op/mod.rs:253 | bug | high | Concat shape inference is missing and falls through to default which returns first input shape | FIXED: sums axis dim across inputs (saturating), validates equal rank + matching non-axis dims. Proving tests concat_sums_the_axis_dimension / concat_rejects_non_axis_dim_mismatch. Also fixed the doubling-overflow the correct impl exposed (test_oom_many_graph_edges). | status=done
src/op/mod.rs:253 | bug | high | Linear shape inference is missing and falls through to default which leaves last dimension as in_features | FIXED: explicit Linear arm replaces last dim with out_features. Proving test linear_sets_last_dim_to_out_features. | status=done
src/op/mod.rs:241 | silent-fallback | medium | Reshape shape inference for target dimension -1 silently falls back to 1 instead of calculating inferred dimension size | FIXED: -1 inferred as total_elems / product(known dims); rejects >1 infer dim and non-divisible cases; saturating product. Proving tests reshape_infers_the_negative_one_dimension / reshape_rejects_non_divisible_infer / reshape_rejects_multiple_infer_dims. Added shared normalize_axis helper (ONE-PLACE, used by Split+Concat). | status=done
src/op/flops.rs:158 | bug | medium | flops_fused_transformer ignores the activation argument and hardcodes Activation::Gelu | FIXED: pass the real activation into FusedFfnBlock. Proving test fused_transformer_flops_respect_activation (Relu != Gelu flops). | status=done
src/op/flops.rs:171 | bug | high | flops_fused_transformer performs direct addition of flops values which can panic on integer overflow | FIXED: saturating_add of attn+ffn flops. | status=done
src/op/hbm.rs:48 | bug | medium | hbm_bytes_read_patch_embed multiplies static position embedding size by batch size | FIXED: removed batch multiplier (pos/cls embeddings are static params read once, like patch weights). | status=done
src/fusion/ffn_block.rs:190 | bug | high | fuse_ffn_block does not add the new fused node to the consumers list of the input tensors | FIXED at the single insertion point: Graph::insert_node now registers the node as a consumer of each input tensor (mirrors builder add_node), fixing both fusion sites (ONE-PLACE). | status=done
src/fusion/attn_block.rs:298 | bug | high | fuse_attention_block does not add the new fused node to the consumers list of the input tensors | FIXED by the same Graph::insert_node change. | status=done
src/fusion/attn_block.rs:217 | bug | high | trace_attention_chain does not verify that intermediate nodes being fused have no consumers outside the fused subgraph | FIXED: post-trace guard rejects the fusion (return None) if any intermediate node output (other than the projection output) has a consumer outside the fused set; internal branching (QKV->split) still allowed. | status=done
src/analysis/bandwidth.rs:225 | bug | medium | truncate slices name strings using byte offsets rather than character boundaries which can panic on Unicode | FIXED: char-based count + take (panic-free, handles max_len==0). | status=done
src/graph/mod.rs:214 | perf | medium | topological_order sorts the queue inside the loop every time an in-degree reaches zero | FIXED: BinaryHeap (max-heap) replaces Vec+resort, O(N log N), same deterministic largest-first order. | status=done
tests/integration_tests.rs:57 | test-gap | low | Integration tests do not assert correctness of input tensor consumer lists after block fusion | FIXED: test_ffn_block_fusion_with_gelu now asserts every fused-node input tensor lists the fused node id in its consumers (proves the insert_node fix). | status=done
tests/break_it.rs:456 | test-gap | high | 30 param rows (Concat _33, Linear _35 across 15 macro invocations) hardcode WRONG hbm_read (Concat 80 vs correct 40 = 2x[5] read; Linear 96 vs correct 88 = 6+12+4 elems) - pre-existing red, verified code correct | FIXED: corrected expected values to 40/88 (code unchanged; hand-verified against hbm_bytes_read_linear/Concat block). | status=done
tests/break_it.rs:146 | test-gap | medium | break_split_no_sections + break_reshape_invalid_dim asserted the OLD lenient mock (empty split=ok/0-outputs; two -1 =>[1,1]) which the correct shape inference now rightly rejects | FIXED: assert is_err (empty sections can't cover the dim; two -1 is ambiguous per numpy/torch). | status=done
tests/adversarial/io_oom.rs:31 | bug | high | test_oom_many_graph_edges: correct Concat shape inference exposed a doubling axis-size that overflowed usize (panic) around iter 53 | FIXED: saturating_add/saturating_mul in Concat/Split/Reshape shape arithmetic. | status=done
DONE 2026-07-17 | src/op/mod.rs:288 | mlgraph | silent-fallback | high | REPOPULATE 2026-07-16: Reshape shape inference without inferred dimensions fails to validate that the input and target shape element counts match, silently returning target shape with mismatched elements count, causing downstream shape mismatch and incorrect memory access size calculations | FIXED: the no-`-1` Reshape branch now returns Error::ShapeMismatch when the fully-specified target's element product != input element count. Proving tests (tests/adversarial/ops.rs): `reshape_rejects_fully_specified_element_count_mismatch` ([2,3]->[4,5] rejected), `reshape_accepts_matching_count_and_infers_negative_one` ([2,3]->[6] ok, ->[-1,2] infers [3,2]). mlgraph green exit 0. | status=done
DONE 2026-07-17 | src/op/mod.rs:323 | mlgraph | silent-fallback | medium | REPOPULATE 2026-07-16: shape inference for element-wise binary operations Add and Mul fails to validate that the two input shapes are identical, silently returning the first input shape on mismatch, leading to downstream shape inconsistencies | FIXED: added an explicit `Op::Add | Op::Mul` arm (was falling through to the first-input-shape default) that requires exactly 2 inputs with identical shapes (no broadcasting exists in this framework) and returns Error::ShapeMismatch otherwise. Proving tests: `elementwise_add_rejects_mismatched_input_shapes`, `elementwise_mul_rejects_mismatched_input_shapes`, `elementwise_add_accepts_identical_input_shapes`. mlgraph green exit 0. | status=donesrc/analysis/bandwidth.rs:77-90 | mlgraph | silent-fallback | high | Claude 2026-07-17: BandwidthAnalysis::analyze silently drops graph elements it cannot resolve. `let Some(node) = graph.node_by_id(*nid) else { continue; }` (77-79) skips any node in the topological order that fails id lookup, and both input_shapes (81-85) and output_shapes (86-90) use `filter_map(|tid| graph.tensor(*tid).map(...))` which silently OMITS any tensor whose id does not resolve. A dangling node/tensor id is a graph-consistency error, but here it produces an UNDERESTIMATED HBM-traffic report with zero signal - and this analysis exists precisely to quantify HBM traffic to motivate fusion, so a silent undercount directly corrupts the decision it feeds (Law 10). Compounding: a node with a missing first input silently defaults dtype to F16 (96-100), halving byte counts vs a real F32 tensor. | analyze() already returns Result - propagate an error (or a loudly-recorded count of unresolved nodes/tensors) when a node/tensor id fails to resolve, rather than continue/filter_map-dropping it. Proving test: a graph with a dangling tensor id yields an error (or a report that explicitly flags N unresolved tensors), NOT a silently-smaller traffic total.
src/analysis/bandwidth.rs:106-110 + :175-183 | mlgraph | correctness | medium | Claude 2026-07-17: arithmetic_intensity is set to 0.0 when hbm_total==0 (108-109), but a node with flops>0 and zero HBM traffic (the ideal fully-fused op keeping all intermediates in SRAM) has effectively INFINITE arithmetic intensity - maximally compute-bound. The 0.0 sentinel collides with genuinely low intensity and is then fed to classify_intensity (175-183) which labels `< 1.0` as "bandwidth-bound" - the exact OPPOSITE of the truth. So the best-fused, most-compute-bound nodes are reported as bandwidth-bound, inverting the signal a fusion analysis is meant to provide. | When hbm_total==0: if flops>0 represent intensity as f64::INFINITY (classified compute-bound), and only report 0.0 (or a distinct "no-op"/"n-a" class) when flops is also 0. Update classify_intensity to handle the infinite/zero-traffic case explicitly. Proving test: a node with flops>0 and hbm_total==0 classifies as compute-bound, not bandwidth-bound.
src/analysis/bandwidth.rs:96-103 | mlgraph | accuracy | medium | Claude 2026-07-17: the byte-cost model uses ONE dtype - the first input tensor's (or F16 default) - and passes it to hbm_bytes_read for ALL input shapes AND hbm_bytes_written for ALL output shapes (102-103). Real ML nodes are frequently mixed-precision: F16 activations with an F32 accumulator/output, INT8/INT4 quantized weights alongside F16 inputs, etc. Billing every tensor of a node at the first input's dtype mis-estimates HBM bytes (e.g. an F32 output counted as F16 halves its true write traffic), skewing the very traffic totals and arithmetic-intensity classifications the pass produces. | Thread per-tensor dtypes through: compute each input/output tensor's bytes at ITS OWN dtype (the TensorMeta already carries dtype, used only for the first input today) rather than a single node-wide dtype. Proving test: a node with an F16 input and an F32 output reports write traffic at F32 byte width, not F16.
